<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.house.springboot.mapper.AdminMapper">
    
    <resultMap type="com.house.springboot.bean.Admin" id="AdminResult">
        <result property="id"    column="id"    />
        <result property="account"    column="account"    />
        <result property="password"    column="password"    />
    </resultMap>

    <sql id="selectAdminVo">
        select id, account, password from admin
    </sql>

    <select id="selectAdminList" parameterType="com.house.springboot.bean.Admin" resultMap="AdminResult">
        <include refid="selectAdminVo"/>
        <where>  
            <if test="account != null  and account != ''"> and account like concat('%', #{account}, '%')</if>
            <if test="password != null  and password != ''"> and password = #{password}</if>
        </where>
    </select>
    
    <select id="selectAdminById" parameterType="Long" resultMap="AdminResult">
        <include refid="selectAdminVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertAdmin" parameterType="com.house.springboot.bean.Admin" useGeneratedKeys="true" keyProperty="id">
        insert into admin
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="account != null">account,</if>
            <if test="password != null">password,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="account != null">#{account},</if>
            <if test="password != null">#{password},</if>
         </trim>
    </insert>

    <update id="updateAdmin" parameterType="com.house.springboot.bean.Admin">
        update admin
        <trim prefix="SET" suffixOverrides=",">
            <if test="account != null">account = #{account},</if>
            <if test="password != null">password = #{password},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteAdminById" parameterType="Long">
        delete from admin where id = #{id}
    </delete>

    <delete id="deleteAdminByIds" parameterType="String">
        delete from admin where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>